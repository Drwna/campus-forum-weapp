# 校园论坛小程序 · 软件项目落地方案

> 项目代号：**CampusHub**
> 形态：微信小程序（学生端） + Web 后台（管理端） + Node.js 后端服务
> 文档版本：v1.1 · 编写日期：2026-05-28

---

## 〇、实现状态（2026-05-28 更新）

### 已完成功能

**后端服务（server）**
- ✅ 微信登录、管理员登录、JWT 鉴权、Token 刷新、登出
- ✅ 用户管理、个人资料更新、资料审核机制
- ✅ 帖子 CRUD、分类权限控制（allowUserPost）
- ✅ 评论 CRUD、子评论支持、敏感词过滤（可配置开关）
- ✅ 点赞/取消点赞（Redis 计数器 + 异步同步）
- ✅ 关注/取消关注
- ✅ 站内消息（评论/点赞/关注/系统通知）
- ✅ 文件上传（Multer + Sharp 缩略图）
- ✅ 公告管理
- ✅ 举报管理（用户举报、管理员处理、结果通知）
- ✅ 管理后台 API（统计、审核、用户、配置管理）
- ✅ 个人资料审核（bio/昵称/性别/年龄）
- ✅ 静态文件服务（/uploads/*）
- ✅ 系统配置管理（app_config 表）

**管理后台（admin）**
- ✅ 登录页面、数据概览（真实统计）
- ✅ 帖子管理（列表、审核、删除）
- ✅ 用户管理（列表、角色切换、禁用/启用）
- ✅ 评论管理（列表、删除）
- ✅ 分类管理（CRUD、allowUserPost 控制）
- ✅ 公告管理（CRUD、置顶）
- ✅ 举报管理（列表、查看被举报内容、解决/驳回、通知举报人）
- ✅ 资料审核（审核 bio/昵称/性别/年龄修改）
- ✅ 系统配置（评论敏感词过滤开关等配置项管理）
- ✅ 统一色彩方案（珊瑚红主题）

**小程序客户端（miniapp）**
- ✅ 首页（帖子信息流、分类筛选、下拉刷新、公告横幅）
- ✅ 帖子详情（内容展示、图片预览、点赞、评论、举报帖子）
- ✅ 发帖页面（选择分类、输入标题/内容、上传图片）
- ✅ 我的页面（用户卡片、帖子/粉丝/关注统计）
- ✅ 消息页面（评论/点赞/关注/系统通知、下拉刷新）
- ✅ 登录页面（微信头像选择、昵称输入、一键登录）
- ✅ 个人资料编辑（bio 走审核、隐私设置即时生效）
- ✅ 我的帖子（审核状态筛选）
- ✅ 我的评论（评论列表、跳转原帖）
- ✅ 公告列表
- ✅ 设置页面
- ✅ 用户主页（用户信息、帖子列表、关注/取消关注）
- ✅ 关注/粉丝列表
- ✅ 评论举报（每条评论右侧举报按钮）
- ✅ 举报结果通知（系统消息）
- ✅ 统一色彩方案（珊瑚红主题）

### 技术实现细节

| 特性 | 实现方式 |
|---|---|
| 数据库 | MySQL 8 + Prisma 7（MariaDB 适配器） |
| 缓存 | Redis 7（帖子缓存、点赞计数器、限流、JWT 黑名单） |
| 鉴权 | JWT 无状态 + Redis 黑名单即时失效 |
| 文件存储 | 本地磁盘 `/data/uploads/` + Express 静态服务 |
| 缩略图 | Sharp 生成 200px/600px/原图三种尺寸 |
| 内容审核 | 评论敏感词过滤（可配置开关，5 分钟缓存刷新） |
| 图片 URL | 服务器返回相对路径，客户端通过 `resolveImageUrl()` 转完整 URL |
| 分类权限 | `allowUserPost` 字段控制（0=仅管理员，1=所有用户） |
| 资料审核 | bio/昵称/性别/年龄修改需管理员审核 |
| 隐私设置 | showGender/showAge 直接生效，无需审核 |
| 举报系统 | 用户举报帖子/评论 → 管理员处理 → 系统消息通知举报人 |
| 系统配置 | `app_config` 表存储，管理后台可动态修改 |

---

## 一、项目规划

### 1.1 项目背景与目标

**业务背景**
高校学生在碎片化时段对校园资讯、社群互动有高频需求。PC 论坛入口受限、独立 App 安装意愿低，校内现有信息化平台普遍以"公告下发"为主，缺少 UGC（用户生成内容）的双向互动能力。微信小程序"免安装、即用即走"的特性匹配此场景，但市面上完整可落地的开源校园论坛参考方案稀缺。

**项目目标（按 SMART 原则量化）**

| 维度 | 指标 |
|---|---|
| 业务目标 | 上线 6 个月内单校月活 ≥ 3000 DAU/MAU 比 ≥ 25% |
| 功能目标 | 覆盖发帖 / 评论 / 点赞 / 关注 / 分类 / 站内消息 6 大场景 |
| 性能目标 | P95 接口响应 < 800 ms；500 并发下错误率 < 0.1% |
| 安全目标 | 通过 OWASP Top 10 自检，敏感词命中率 ≥ 95% |
| 工程目标 | CI/CD 全自动化，单次发版回滚时间 < 5 分钟 |

**非目标（明确排除，控制范围）**
- 不做即时聊天 / IM（后续接入腾讯 IM SDK，不在 v1）
- 不做支付与交易闭环（v1 不涉及二手交易资金流）
- 不做跨校多租户（单校单实例部署，多校通过部署多套解决）

---

### 1.2 核心功能模块拆分

```
CampusHub
├── 用户域 (User Domain)
│   ├── 微信授权登录 / 用户信息管理
│   ├── 关注 & 粉丝关系
│   └── 个人主页 / 我的内容聚合
├── 内容域 (Content Domain)
│   ├── 帖子：发布 / 编辑 / 删除 / 详情 / 列表 / 搜索
│   ├── 评论：一级评论 / 二级回复 / 删除
│   ├── 分类：分类导航 / 分类下帖子流
│   └── 媒体：图片上传 / 本地存储 / 缩略图生成 / 静态服务
├── 互动域 (Interaction Domain)
│   ├── 点赞 / 取消点赞（幂等保证）
│   ├── 收藏（v1.1 预留）
│   └── 站内消息：评论通知 / 点赞通知 / 关注通知
├── 治理域 (Moderation Domain)
│   ├── 帖子审核（v1 先审后发，预留配置切换）
│   ├── 评论审核 / 举报处置
│   ├── 敏感词过滤（DFA + 微信内容安全 API 双校验）
│   └── 用户封禁 / 解封
├── 运营域 (Operation Domain)
│   ├── 系统公告 / 全站 Banner
│   ├── 分类管理（CRUD）
│   ├── 图片管理（列表 / 预览 / 删除 / 孤儿清理 / 容量统计）
│   └── 操作日志 / 数据看板（PV/UV/发帖量）
└── 平台域 (Platform Domain - 跨切面)
    ├── 鉴权 (JWT) / RBAC 权限
    ├── 限流 / 防刷 / 风控
    ├── 日志 / 监控 / 告警
    └── 配置中心（环境变量 + 数据库配置表）
```

---

### 1.3 用户角色与使用流程

**角色矩阵**

| 角色 | 入口 | 关键权限 | 鉴权方式 |
|---|---|---|---|
| 游客 | 小程序 | 浏览帖子（部分校园限制内容遮蔽）、查看公告 | 无 |
| 普通用户（学生） | 小程序 | 发帖 / 评论 / 点赞 / 关注 / 个人中心 | 微信 OpenID + JWT |
| 内容审核员 | Web 后台 | 帖子审核、评论删除、举报处置 | 账密 + JWT + RBAC |
| 超级管理员 | Web 后台 | 全部审核员权限 + 用户封禁 + 分类 / 公告 / 日志 | 账密 + JWT + RBAC |

**典型流程：发帖 → 审核 → 上线**

```
学生侧                后端                 审核员                Nginx/DB
 │                    │                     │                     │
 ├─ wx.login(code) ──►│                     │                     │
 │                    ├─ code2Session ─────────────────────────► 微信开放平台
 │◄── JWT token ──────┤                     │                     │
 │                    │                     │                     │
 ├─ POST /api/v1/posts ─►│                     │                     │
 │  (含图片临时URL)    ├─ 敏感词预扫 ────────────────────────────►│
 │                    ├─ 入库 status=PENDING│                     │
 │◄── postId ─────────┤                     │                     │
 │                    │                     │                     │
 │                    │◄── GET /api/v1/admin/posts?status=pending ──┤
 │                    ├──── 待审列表 ──────────────────────────►│
 │                    │                     │                     │
 │                    │◄── PUT /api/v1/admin/posts/:id/status ───┤
 │                    ├─ status=APPROVED → 写入推送消息队列      │
 │                    ├─ 异步落入 inbox（被关注者）              │
 │                    │                     │                     │
 │◄── 站内消息 ────────┤                     │                     │
```

---

### 1.4 技术选型

| 层 | 技术 | 版本 | 选型理由 |
|---|---|---|---|
| **小程序前端** | uni-app + Vue 3 + JavaScript | uni-app 3.x | 一次开发多端发布；Vue 3 Composition API 易测试 |
| | Pinia | 2.x | 替代 Vuex，TS 友好，体积更小 |
| | Vite | 5.x | 构建速度优于 Webpack |
| **Web 后台前端** | Vue 3 + Vite + TypeScript | - | 与小程序端共享部分逻辑 |
| | Element Plus | 2.x | 后台中后台 UI 标杆 |
| | Vue Router 4 + Pinia | - | 路由守卫做前端 RBAC |
| | ECharts | 5.x | 数据可视化 |
| **后端** | Node.js | 20 LTS | V8 性能与安全更新 |
| | Express | 4.22+ | 中间件生态成熟 |
| | TypeScript | 5.x | 与前端共享类型定义（DTO） |
| | Prisma | 7.x | 类型安全 + Migration 管理 + MariaDB 适配器 |
| **数据存储** | MySQL | 8.0 | 事务 + 全文索引（中文需 ngram） |
| | Redis | 7.x | 缓存 + 限流 + 分布式锁 + 计数器（点赞数最终一致性） |
| | 本地磁盘存储 | - | 图片落盘到服务器 `/data/uploads`，由 Express 静态分发 |
| | Multer + Sharp | latest | Multer 处理 multipart 上传；Sharp 生成多尺寸缩略图（200/600/原图） |
| **基础设施** | Docker + docker-compose | - | 本地与生产环境一致 |
| | Nginx | 1.24+ | 反向代理 / SSL / 静态资源 |
| | PM2 | latest | Node 进程守护 + 集群模式 |

> 关键升级点（论文未提）：**Redis（缓存与计数器）、本地磁盘存储 + Express 静态分发（图片）、Prisma 7（取代裸 SQL）、TypeScript（前后端类型同源）**。

---

### 1.5 系统整体架构设计

```
┌────────────────────────────────────────────────────────────────┐
│                      接入层 (Access Layer)                      │
│   微信小程序 (uni-app)        管理后台 (Vue3 + Element Plus)    │
└──────────────┬─────────────────────────────┬───────────────────┘
               │ HTTPS                        │ HTTPS
               ▼                              ▼
┌────────────────────────────────────────────────────────────────┐
│                  网关层 (Nginx + Rate Limit)                    │
│        SSL 终止 / 静态资源 / 反向代理 / IP 限流 / 防 CC         │
└──────────────────────────────┬─────────────────────────────────┘
                               │
                               ▼
┌────────────────────────────────────────────────────────────────┐
│              应用层 (Node.js + Express, PM2 cluster)            │
│  ┌──────────────────────────────────────────────────────────┐ │
│  │ Routes      → Controllers → Services → Repositories       │ │
│  │ Middleware: auth(JWT) / rbac / validate(zod) / logger /   │ │
│  │             rateLimit(Redis) / errorHandler / traceId     │ │
│  └──────────────────────────────────────────────────────────┘ │
└────┬───────────────┬───────────────┬───────────────┬───────────┘
     │               │               │               │
     ▼               ▼               ▼               ▼
┌─────────┐    ┌─────────┐    ┌──────────┐    ┌──────────┐
│ MySQL   │    │ Redis   │    │ 本地磁盘 │    │ 微信开放 │
│ (主数据) │    │(缓存/锁)│    │/data/    │    │   平台   │
│         │    │         │    │ uploads  │    │          │
└─────────┘    └─────────┘    └──────────┘    └──────────┘
                                    ▲
                                    │ Nginx /uploads 静态路由
                                    │ sendfile + expires + 防盗链

旁路：Prometheus（指标）/ Loki（日志）/ Sentry（异常）
```

**分层职责**

- **接入层**：UI 渲染、表单校验、本地缓存、状态管理。**不放业务规则**。
- **网关层**：SSL、静态资源、反代、**Nginx IP 级限流**（防 CC、防扫描，粗粒度）。
- **应用层**：路由 → 控制器（编排） → 服务（业务规则） → 仓储（数据访问）。**业务规则只在 Service 层**。
- **数据层**：MySQL 持久化主数据；Redis 缓存热点 / 计数器 / 限流；本地磁盘 `/data/uploads` 存储用户图片，Nginx 直接通过 `/uploads/*` 路由静态分发；微信平台仅做身份核验。

---

### 1.6 项目开发阶段划分（里程碑）

| 阶段 | 周期 | 交付物 | 完成标准 | 状态 |
|---|---|---|---|---|
| **M0 立项与设计** | W1-W2（2周） | 需求文档 / 架构图 / DB 设计 / API 契约 | 评审通过、Mock 服务可用 | ✅ 已完成 |
| **M1 基础框架** | W3-W4（2周） | 项目骨架 / CI/CD / 鉴权中间件 / 部署脚本 | 健康检查接口可访问、流水线绿色 | ✅ 已完成 |
| **M2 核心 MVP** | W5-W8（4周） | 登录 / 发帖 / 评论 / 点赞 / 详情页 | 端到端跑通，可内部体验 | ✅ 已完成 |
| **M3 治理与后台** | W9-W11（3周） | 审核 / 封禁 / 分类 / 公告 / 日志 / Web 后台 | 后台可独立完成内容运营 | ✅ 已完成 |
| **M4 完善与体验** | W12-W13（2周） | 关注 / 站内消息 / 个人中心 / 搜索 | 全功能联通，性能初调 | ✅ 已完成 |
| **M5 联调测试** | W14-W15（2周） | 功能测试 / 压测 / 安全扫描 / Bug 修复 | 通过率 100%，性能达标 | 进行中 |
| **M6 灰度上线** | W16（1周） | 灰度 5% → 50% → 100%、数据看板 | 无 P0 故障稳定运行 7 天 | 待开始 |

> **当前进度**：M0-M4 已完成，核心功能全部实现。M5 联调测试进行中。

---

## 二、系统设计文档

### 2.1 系统架构说明（工程化视角）

**部署拓扑（v1 单机版）**

```
[ 微信小程序 ]──┐
                ├──HTTPS──► [ Nginx :443 ] ──► [ Node x 2 (PM2) :3001 ] ──► [ MySQL :3306 ]
[ 管理后台 ]────┘                │                       │                  ─► [ Redis :6379 ]
                                 │                       └──写入──► /data/uploads/
                                 │                                       ▲
                                 └──/uploads/* 直接静态返回（sendfile）──┘
```

**部署拓扑（v2 流量增长后）**

```
              ┌─ Node A ─┐
[ SLB / CLB ]─┼─ Node B ─┼─► [ MySQL 主从 ] + [ Redis 哨兵 ] + [ MQ ]
              └─ Node C ─┘         │
                                   └─► NFS / 共享文件系统（多节点共用 /data/uploads）
                                        或迁移至 MinIO 私有对象存储
```

**v1 → v2 迁移要点**
- **图片存储**：本地磁盘 → NFS 共享挂载（最简方案，rsync 同步存量文件）或迁移至 MinIO（需改造 `infra/storage/` 层，`file.url` 改为对象存储 URL）
- **数据库**：单实例 → 主从复制，写走主库、读走从库；Prisma 连接串指向 ProxySQL / 读写分离代理
- **缓存**：单机 Redis → Redis 哨兵 / Cluster，应用层 ioredis 配置 sentinel 地址即可切换
- **进程**：PM2 cluster → Docker + K8s Deployment，探针用 `/healthz`

**关键架构决策（ADR 摘要）**

| 决策 | 选择 | 理由 | 备选 |
|---|---|---|---|
| ADR-001 | 单体后端 | 用户规模 < 5 万，微服务收益 < 复杂度成本 | 微服务 |
| ADR-002 | JWT 无状态鉴权 | 横向扩容友好；Token 失效用 Redis 黑名单 | Session |
| ADR-003 | v1 帖子先审后发 | 校园 UGC 有内容合规压力，MVP 优先降低治理风险；后续可通过 `app_config.review_mode` 切换为先发后审 | 先发后审 |
| ADR-004 | 计数器走 Redis | like_count 高频读写若直接打 MySQL 会成瓶颈 | DB 原子更新 |
| ADR-005 | 图片本地磁盘存储 | 校园场景流量可控、运维简单、零外部费用、便于后台统一管理与审计；Nginx sendfile 性能足够；后续可平滑切 MinIO/COS | 对象存储（COS/OSS） |
| ADR-006 | 后端中转上传（非直传） | 本地存储无法做客户端直传；中转便于在写入前完成鉴权、大小校验、Sharp 缩略图生成、敏感图片预扫 | 客户端直传 |

---

### 2.2 模块详细设计

每个模块格式：**职责 / 输入 / 输出 / 依赖 / 关键规则**。

#### 2.2.1 用户模块（User Module）

- **职责**：用户身份维护、登录态管理、关注关系
- **输入**：微信 code、JWT、用户编辑表单
- **输出**：用户基础信息 DTO、Token、关注 / 粉丝列表
- **依赖**：微信开放平台（jscode2session）、MySQL、Redis
- **关键规则**：
  - 首次登录自动注册，OpenID 唯一索引保证幂等
  - 用户被封禁（status=1）后，存量 Token 通过 Redis 黑名单立即失效
  - 关注操作幂等（唯一索引 + UPSERT）

#### 2.2.2 帖子模块（Post Module）

- **职责**：帖子全生命周期、列表 / 搜索 / 详情
- **输入**：发帖 DTO、查询参数（categoryId、keyword、page、size）
- **输出**：帖子 DTO、分页结果
- **依赖**：MySQL、Redis（缓存 + 计数器）、敏感词服务、本地存储模块（StorageService）
- **关键规则**：
  - 列表查询走 Redis 缓存（5 分钟 TTL，发帖时主动失效）
  - 搜索关键词走 MySQL 全文索引（ngram 解析器，支持中文）
  - 自有帖子编辑：检查 user_id 归属
  - 软删除（status=3），物理删除由清理任务完成

#### 2.2.3 评论模块（Comment Module）

- **职责**：一/二级评论 CRUD、回复树构建
- **输入**：评论 DTO（postId / content / parentId）
- **输出**：评论树 DTO（嵌套两级）
- **依赖**：MySQL、Redis、消息推送服务
- **关键规则**：
  - parent_id=0 为一级评论；二级评论的 parent_id 指向一级评论 ID（不做无限嵌套）
  - 评论新增时异步触发 inbox 消息（被评论的帖主、被回复的评论作者）

#### 2.2.4 互动模块（Like / Follow）

- **职责**：点赞、关注的幂等操作
- **输入**：JWT、targetId
- **输出**：操作后的最新计数
- **依赖**：MySQL（持久化）、Redis（计数器 + 分布式锁）
- **关键规则**：
  - **写策略**：先写 Redis（INCR / DECR）+ 写日志队列，异步落 MySQL（每 5 秒批量同步）
  - **读策略**：列表页 like_count 直接读 Redis；缓存 miss 时回源 MySQL
  - **幂等保证**：`UNIQUE KEY (user_id, post_id)`，重复点赞返回当前值即可

#### 2.2.5 审核模块（Moderation Module）

- **职责**：内容审核、敏感词检测、举报处置
- **输入**：帖子 / 评论文本、举报记录
- **输出**：审核状态、驳回原因
- **依赖**：DFA 敏感词树、微信内容安全 API、MySQL
- **关键规则**：
  - 用户提交时**预扫**：DFA 命中直接拒绝；微信 API 命中直接拒绝
  - 通过预扫后入库 status=PENDING；管理员人工复核
  - 命中违规词触发自动举报记录，便于后续训练

#### 2.2.6 站内消息模块（Inbox Module）

- **职责**：评论 / 点赞 / 关注事件的消息聚合
- **输入**：业务事件（CommentCreated / LikeCreated / FollowCreated）
- **输出**：用户收件箱列表、未读数
- **依赖**：MySQL（message 表）、Redis（未读数缓存）
- **关键规则**：
  - 自己对自己的操作不产生消息
  - 同一 (来源用户, 目标对象) 1 小时内的点赞合并为一条（防刷屏）

---

### 2.3 数据库设计

> 在论文 8 张表基础上**补全工程必需表**：消息表、举报表、敏感词表、文件表。

#### 2.3.1 完整表清单

| 表名 | 用途 | 论文 | 新增 |
|---|---|:-:|:-:|
| `user` | 用户主表 | ✓ | |
| `category` | 分类 | ✓ | |
| `post` | 帖子 | ✓ | |
| `comment` | 评论 | ✓ | |
| `post_like` | 点赞（避免 like 关键字） | ✓ | |
| `user_follow` | 关注关系（避免 follow 关键字） | ✓ | |
| `notice` | 系统公告 | ✓ | |
| `admin_log` | 管理员操作日志 | ✓ | |
| `message` | 站内消息 | | ✓ |
| `report` | 举报记录 | | ✓ |
| `sensitive_word` | 敏感词库（运营可配） | | ✓ |
| `file` | 文件元信息（图片管理） | | ✓ |
| `app_config` | 系统配置（运营可调） | | ✓ |

#### 2.3.2 关键表 DDL（节选 - 工程化版）

> **注意**：以下 DDL 为工程化版本，与论文原始设计有差异的地方已在注释中标明。所有表均使用 `utf8mb4` 字符集。`comment.status` 与 `post.status` 语义不同——前者仅表示正常/删除，后者表示审核流转状态。

```sql
-- 用户表
CREATE TABLE `user` (
  `user_id`        BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
  `wechat_openid`  VARCHAR(64)     DEFAULT NULL COMMENT '微信OpenID；普通用户必填',
  `union_id`       VARCHAR(64)     DEFAULT NULL COMMENT '微信UnionID',
  `admin_account`  VARCHAR(64)     DEFAULT NULL COMMENT '管理员账号；管理员必填',
  `nickname`       VARCHAR(64)     NOT NULL,
  `avatar`         VARCHAR(512)    DEFAULT NULL,
  `bio`            VARCHAR(200)    DEFAULT NULL COMMENT '个性签名',
  `gender`         TINYINT         DEFAULT 0 COMMENT '0未知/1男/2女',
  `role`           TINYINT         NOT NULL DEFAULT 0 COMMENT '0普通/1审核员/2超管',
  `status`         TINYINT         NOT NULL DEFAULT 0 COMMENT '0正常/1禁用',
  `password_hash`  VARCHAR(128)    DEFAULT NULL COMMENT 'bcrypt; 仅管理员',
  `last_login_at`  DATETIME        DEFAULT NULL,
  `create_time`    DATETIME        NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `update_time`    DATETIME        NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  `deleted_at`     DATETIME        DEFAULT NULL COMMENT '软删除',
  PRIMARY KEY (`user_id`),
  UNIQUE KEY `uk_openid` (`wechat_openid`),
  UNIQUE KEY `uk_admin_account` (`admin_account`),
  KEY `idx_role_status` (`role`, `status`),
  KEY `idx_create_time` (`create_time`),
  CONSTRAINT `ck_user_identity` CHECK (
    (`role` = 0 AND `wechat_openid` IS NOT NULL AND `admin_account` IS NULL)
    OR
    (`role` IN (1, 2) AND `admin_account` IS NOT NULL AND `password_hash` IS NOT NULL)
  )
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='用户表';

-- 帖子表
CREATE TABLE `post` (
  `post_id`        BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
  `user_id`        BIGINT UNSIGNED NOT NULL,
  `category_id`    INT UNSIGNED    NOT NULL,
  `title`          VARCHAR(100)    NOT NULL,
  `content`        TEXT            NOT NULL,
  `image_urls`     JSON            DEFAULT NULL COMMENT '改用JSON数组',
  `like_count`     INT UNSIGNED    NOT NULL DEFAULT 0,
  `comment_count`  INT UNSIGNED    NOT NULL DEFAULT 0,
  `view_count`     INT UNSIGNED    NOT NULL DEFAULT 0,
  `status`         TINYINT         NOT NULL DEFAULT 0 COMMENT '0待审/1通过/2驳回/3删除',
  `reject_reason`  VARCHAR(200)    DEFAULT NULL,
  `is_top`         TINYINT         NOT NULL DEFAULT 0,
  `create_time`    DATETIME        NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `update_time`    DATETIME        NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`post_id`),
  KEY `idx_user`        (`user_id`, `create_time` DESC),
  KEY `idx_cat_status`  (`category_id`, `status`, `create_time` DESC),
  KEY `idx_status_top`  (`status`, `is_top` DESC, `create_time` DESC),
  FULLTEXT KEY `ft_title_content` (`title`, `content`) WITH PARSER ngram
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='帖子表';

-- 分类表
CREATE TABLE `category` (
  `category_id`    INT UNSIGNED    NOT NULL AUTO_INCREMENT,
  `category_name`  VARCHAR(50)     NOT NULL COMMENT '分类名称',
  `category_desc`  VARCHAR(200)    DEFAULT NULL COMMENT '分类说明',
  `sort_order`     INT             NOT NULL DEFAULT 0 COMMENT '排序权重',
  `status`         TINYINT         NOT NULL DEFAULT 0 COMMENT '0启用/1停用',
  `create_time`    DATETIME        NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `update_time`    DATETIME        NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`category_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='帖子分类';

-- 站内消息（论文未提，工程必需）
CREATE TABLE `message` (
  `message_id`   BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
  `to_user_id`   BIGINT UNSIGNED NOT NULL,
  `from_user_id` BIGINT UNSIGNED DEFAULT NULL COMMENT '系统消息为NULL',
  `type`         TINYINT         NOT NULL COMMENT '1评论/2回复/3点赞/4关注/5系统',
  `target_type`  TINYINT         NOT NULL COMMENT '1帖子/2评论',
  `target_id`    BIGINT UNSIGNED NOT NULL,
  `content`      VARCHAR(500)    DEFAULT NULL,
  `is_read`      TINYINT         NOT NULL DEFAULT 0,
  `create_time`  DATETIME        NOT NULL DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`message_id`),
  KEY `idx_to_unread` (`to_user_id`, `is_read`, `create_time` DESC)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='站内消息';

-- 文件元信息（论文未提，本地存储 + 后台管理需要）
CREATE TABLE `file` (
  `file_id`        BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
  `user_id`        BIGINT UNSIGNED NOT NULL COMMENT '上传者',
  `original_name`  VARCHAR(255)    NOT NULL COMMENT '原始文件名（审计用）',
  `storage_type`   VARCHAR(16)     NOT NULL DEFAULT 'local' COMMENT 'local / cos / oss，预留切换',
  `relative_path`  VARCHAR(255)    NOT NULL COMMENT '相对 /data/uploads 的路径，如 post/2026/05/01/uuid.jpg',
  `thumb_path`     VARCHAR(255)    DEFAULT NULL COMMENT '缩略图相对路径',
  `url`            VARCHAR(512)    NOT NULL COMMENT '对外访问 URL，如 /uploads/post/2026/05/01/uuid.jpg',
  `mime_type`      VARCHAR(64)     NOT NULL,
  `size_bytes`     INT UNSIGNED    NOT NULL,
  `width`          INT             DEFAULT NULL,
  `height`         INT             DEFAULT NULL,
  `hash_md5`       CHAR(32)        DEFAULT NULL COMMENT '文件 MD5，用于秒传/查重',
  `business`       VARCHAR(32)     NOT NULL COMMENT 'avatar / post / notice / ...',
  `ref_count`      INT UNSIGNED    NOT NULL DEFAULT 0 COMMENT '被引用次数',
  `status`         TINYINT         NOT NULL DEFAULT 0 COMMENT '0已上传/1引用中/2孤儿/3已删除',
  `create_time`    DATETIME        NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `update_time`    DATETIME        NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`file_id`),
  KEY `idx_user`         (`user_id`),
  KEY `idx_business`     (`business`, `status`),
  KEY `idx_create_time`  (`create_time`),
  KEY `idx_hash`         (`hash_md5`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='文件元信息';
```

#### 2.3.3 索引策略

| 场景 | 索引 |
|---|---|
| 首页帖子流（按时间倒序） | `post(status, is_top desc, create_time desc)` |
| 分类下帖子 | `post(category_id, status, create_time desc)` |
| 用户的帖子 | `post(user_id, create_time desc)` |
| 关键词搜索 | `FULLTEXT(title, content) WITH PARSER ngram` |
| 帖子下评论分页 | `comment(post_id, parent_id, create_time)` |
| 我的未读消息 | `message(to_user_id, is_read, create_time desc)` |
| 防重复点赞 | `UNIQUE(user_id, post_id)` on `post_like` |

#### 2.3.4 容量与性能预估

| 表 | 初期 | 1年后 | 应对 |
|---|---|---|---|
| user | 1万 | 5万 | 单表足够 |
| post | 5万 | 30万 | 单表足够，归档 status=3 |
| comment | 20万 | 200万 | 单表足够，必要时按 post_id hash 分表 |
| post_like | 50万 | 500万 | 走 Redis 计数 + 异步落库 |
| message | 50万 | 500万 | 按 to_user_id 分表 + 30天前归档 |

---

### 2.4 接口设计（RESTful）

#### 2.4.1 全局规约

- **协议**：HTTPS only
- **格式**：`Content-Type: application/json; charset=utf-8`
- **鉴权**：`Authorization: Bearer <jwt>`
- **统一响应**：

```json
{
  "code": 0,
  "message": "ok",
  "data": { },
  "traceId": "8f3e..."
}
```

- **HTTP 状态码**：200 业务正常 / 400 参数错 / 401 未登录 / 403 无权限 / 404 不存在 / 429 限流 / 500 服务异常
- **分页**：`?page=1&size=20`，响应含 `{ list, total, page, size, hasMore }`
- **错误码**：四位数，前两位为模块（10 用户 / 20 帖子 / 30 评论 / 40 互动 / 50 治理 / 90 系统）

**错误码表**

| 码 | 含义 | HTTP |
|---|---|---|
| 1001 | 用户不存在 | 404 |
| 1002 | 用户已被禁用 | 403 |
| 1003 | 微信登录失败 | 401 |
| 1004 | 管理员密码错误（含锁定提示） | 401 |
| 2001 | 帖子不存在 | 404 |
| 2002 | 无权操作他人帖子 | 403 |
| 2003 | 帖子含敏感内容被拒 | 422 |
| 3001 | 评论不存在 | 404 |
| 3002 | 无权删除他人评论 | 403 |
| 4001 | 已点赞 / 已关注（幂等提示） | 200 |
| 4002 | 未点赞 / 未关注（取消操作） | 200 |
| 5001 | 审核状态非法 | 400 |
| 5002 | 分类下仍有帖子，不可删除 | 409 |
| 9001 | 限流触发 | 429 |
| 9002 | 服务内部异常 | 500 |

#### 2.4.2 接口示例

**[POST] /api/v1/auth/wechat-login** — 微信登录

```json
// Request
{ "code": "081abcXY..." }

// Response 200
{
  "code": 0,
  "data": {
    "token": "eyJhbGciOi...",
    "expiresIn": 7200,
    "refreshToken": "rfk_...",
    "user": {
      "userId": "1024",
      "nickname": "张三",
      "avatar": "https://cdn.../a.png",
      "role": 0
    }
  }
}
```

**[GET] /api/v1/posts** — 帖子列表

```
GET /api/v1/posts?categoryId=2&keyword=食堂&page=1&size=20
Authorization: Bearer <token>   (可选，登录态返回 isLiked 字段)
```

**[POST] /api/v1/posts** — 发帖

```json
{
  "title": "食堂三楼新菜推荐",
  "content": "今天吃到的...",
  "categoryId": 2,
  "imageUrls": ["https://cdn.../1.jpg"]
}
```

**[POST] /api/v1/posts/:postId/like** — 点赞（替代 paper 中 `/api/likes`，更 RESTful）

```json
// Response
{ "code": 0, "data": { "liked": true, "likeCount": 42 } }
```

**[GET] /api/v1/admin/posts** — 管理员帖子列表（需 role >= 1）

```
GET /api/v1/admin/posts?status=PENDING&page=1&size=20
```

**[POST] /api/v1/files/upload** — 图片上传（multipart/form-data）

```
// Request: form-data, 字段名 file
// Response
{ "code": 0, "data": { "url": "/uploads/post/2026/05/01/xxx.jpg", "thumbUrl": "/uploads/post/2026/05/01/xxx_thumb.jpg" } }
```

> **完整接口共约 50 个**，按 `/api/v1/{module}/{resource}` 组织，OpenAPI 3.0 规范托管在 `docs/openapi.yaml`，配合 Swagger UI 提供在线调试。所有对外 DTO 中的 `BIGINT` ID 一律输出为字符串，避免小程序 / JavaScript number 精度丢失。

---

### 2.5 关键业务流程

#### 2.5.1 微信登录时序

```
小程序        后端                微信开放平台      MySQL       Redis
  │             │                       │             │           │
  ├─wx.login()                          │             │           │
  ├──────POST /auth/wechat-login───────►│             │           │
  │             ├── jscode2session ────►│             │           │
  │             │◄── openid+session ────┤             │           │
  │             ├─ SELECT by openid ───────────────►  │           │
  │             │◄── user 或 null ──────────────────  │           │
  │             ├─ 不存在则 INSERT ────────────────►  │           │
  │             ├─ 生成 JWT (userId,role,exp)         │           │
  │             ├─ 写 refreshToken ─────────────────────────────► │
  │◄────token + user info ──────────────┤             │           │
```

#### 2.5.2 点赞的最终一致性方案

```
用户点赞
  │
  ▼
后端校验 JWT
  │
  ▼
Redis Lua 原子脚本：
  - 若 like:{userId}:{postId} 已存在，直接返回当前 likeCount
  - SET like:{userId}:{postId}=1
  - INCR post:{postId}:likeCount
  - XADD like_sync_stream userId postId op=like ts
  │
  ▼
返回 { liked:true, likeCount:<最新值> }

异步 Worker：
  Redis Stream consumer group 批量消费 like_sync_stream
  → MySQL 按 (user_id, post_id) UPSERT post_like
  → 批量聚合或定期校准 post.like_count
  → 成功后 ACK；失败保留待重试，超过阈值进入死信记录
```

> **队列可靠性**：v1 直接使用 Redis Stream + consumer group + ACK，避免 Redis List 在 Worker 崩溃时难以确认消费状态的问题。Redis 开启 AOF 持久化；Worker 由 PM2 守护，重启后继续消费未 ACK 消息。v2 可切换为 RabbitMQ / Kafka。

#### 2.5.3 帖子发布与审核流程

详见 1.3 节流程图。**关键控制点**：
- 客户端：通过 `POST /api/v1/files/upload` 把图片上传到后端 → 后端写入 `/data/uploads/post/yyyy/mm/dd/{uuid}.jpg`，同步生成 thumb/600 缩略图 → 返回相对 URL（如 `/uploads/post/2026/05/01/xxx.jpg`） → 客户端再调发帖接口提交图片 URL
- 后端：DFA 预扫 → 微信内容安全（图片走 imgSecCheck） → 入库 PENDING + 在 file 表标记 status=1 引用中 → 通知审核员
- 审核：通过则 status=APPROVED 并触发"被关注者 inbox" + 缓存失效；驳回则记 reject_reason 并发消息给作者
- 帖子删除：file 表对应记录 status 回退为 2 孤儿，由 `orphanFileClean.job` 在 7 天后物理删除磁盘文件

---

### 2.6 安全与性能设计

#### 2.6.1 安全控制矩阵

| 威胁 | 控制措施 |
|---|---|
| **身份伪造** | JWT + RS256；refreshToken 持久化于 Redis 可主动撤销 |
| **越权访问** | 后端二次校验资源归属（`post.user_id == jwt.userId`），不依赖前端 |
| **SQL 注入** | Prisma / 参数化查询；杜绝字符串拼接 |
| **XSS** | 入库前过滤 `<script>` 等危险标签（DOMPurify）；前端使用文本渲染而非 v-html |
| **CSRF** | 小程序无 Cookie 不受影响；管理后台用 JWT + SameSite |
| **暴力破解** | 管理员密码错误 3 次锁定 15 分钟；IP + 账号双维度限流 |
| **接口刷量** | Redis 限流：用户 60 次/分钟，IP 200 次/分钟，发帖 5 次/分钟 |
| **图片越权** | Nginx referer 防盗链 + 管理后台通过后端代理预览（不直接暴露原始 URL） |
| **内容违规** | DFA + 微信内容安全 API 双校验；用户举报通道 |
| **数据泄露** | DB 备份每日全量 + binlog 增量；密码 bcrypt（cost=10） |

#### 2.6.2 性能设计

| 优化项 | 措施 | 预期收益 |
|---|---|---|
| 列表查询 | Redis 缓存 5 分钟 | DB 压力 ↓ 80% |
| 计数器 | Redis 实时 + 异步落库 | 写 QPS ↑ 10x |
| 图片 | 本地磁盘 + Nginx sendfile + 缩略图（Sharp） | 后端免于图片分发，IO 直接走内核 |
| 慢查询 | 全 SQL 走 EXPLAIN，禁止全表扫 | P99 延迟 < 200ms |
| 连接池 | Prisma 连接池（pool 20）+ Redis pool 50 | 高并发下连接复用 |
| 进程模型 | PM2 cluster (CPU 核数) | 充分利用多核 |
| 静态资源 | gzip + brotli 压缩 | 传输体积 ↓ 50% |

---

## 三、开发实施方案

### 3.1 技术栈具体说明

| 子项目 | 主要依赖 |
|---|---|
| `miniapp/` | uni-app 3, Vue 3, JavaScript, Pinia, uni.request 封装 |
| `admin/` | Vue 3, Vite, TypeScript, Element Plus, Pinia, Vue Router 4, ECharts, Axios |
| `server/` | Node.js 20, Express 4.22, TypeScript 5, Prisma 7, jsonwebtoken, bcryptjs, ioredis, zod, winston, helmet, cors, multer, sharp, express-rate-limit |

> **说明**：三个项目独立管理依赖，无 monorepo。共享类型在各项目 `src/shared/` 下独立维护。

---

### 3.2 项目目录结构

#### 3.2.1 项目总览（三个独立项目）

```
campus-forum-weapp/
├── admin/                      # Web 管理后台
├── server/                     # Node 后端
├── miniapp/                    # 微信小程序（uni-app）
├── docs/
│   └── openapi.yaml            # API 契约
├── deploy/
│   ├── docker-compose.dev.yml  # 本地开发 MySQL+Redis
│   └── nginx.conf
├── .env.example                # 环境变量模板
├── AGENTS.md                   # 开发指南
├── README.md                   # 项目说明
└── 项目落地方案.md              # 完整项目方案
```

#### 3.2.2 后端目录（`server/`）

```
src/
├── app.ts                      # Express 实例装配 + 启动入口
├── shared/                     # 共享类型、错误码、枚举
│   ├── errors.ts               # 错误码定义
│   ├── enums.ts                # 状态枚举
│   ├── response.ts             # 响应结构
│   └── ids.ts                  # BigInt ID 转换
├── middleware/
│   ├── auth.ts                 # JWT 解析
│   ├── rbac.ts                 # 角色校验
│   ├── validate.ts             # zod 参数校验
│   ├── rateLimit.ts            # Redis 滑动窗口限流
│   ├── traceId.ts              # 请求追踪 ID
│   └── errorHandler.ts         # 全局错误处理
├── modules/                    # 按业务域聚合
│   ├── auth/                   # 认证模块
│   ├── user/                   # 用户模块
│   ├── post/                   # 帖子模块
│   ├── comment/                # 评论模块
│   ├── like/                   # 点赞模块
│   ├── follow/                 # 关注模块
│   ├── category/               # 分类模块
│   ├── message/                # 消息模块
│   ├── notice/                 # 公告模块
│   ├── profile/                # 资料审核模块
│   ├── file/                   # 文件上传模块
│   ├── moderation/             # 内容审核模块
│   ├── report/                 # 举报模块
│   └── admin/                  # 管理后台 API
├── infra/
│   ├── prisma/client.ts        # Prisma 客户端（MariaDB 适配器）
│   ├── redis/client.ts         # Redis 客户端
│   └── moderation/             # DFA 敏感词过滤
└── utils/
    ├── logger.ts               # Winston 日志
    ├── errors.ts               # AppError 类
    ├── response.ts             # 响应工具
    └── notify.ts               # 消息通知工具
```

#### 3.2.3 小程序目录（`miniapp/`）

```
src/
├── main.js                     # 应用入口
├── App.vue                     # 应用根组件
├── pages.json                  # 路由配置
├── manifest.json               # 应用配置
├── pages/                      # 页面
│   ├── index/                  # 首页
│   ├── detail/                 # 帖子详情
│   ├── publish/                # 发帖
│   ├── mine/                   # 我的
│   ├── message/                # 消息
│   ├── login/                  # 登录
│   ├── editprofile/            # 编辑资料
│   ├── myposts/                # 我的帖子
│   ├── mycomments/             # 我的评论
│   ├── notices/                # 公告
│   ├── settings/               # 设置
│   ├── user/                   # 用户主页
│   └── followlist/             # 关注列表
├── api/                        # API 接口
├── stores/                     # Pinia 状态管理
├── utils/                      # 工具函数
│   ├── http.js                 # 请求封装 + 图片 URL 解析
│   └── ...
└── static/                     # 静态资源
```

---

### 3.3 开发流程（开发 → 测试 → 部署）

#### 3.3.1 分支模型（GitHub Flow 简化）

```
main         ────────●────────●────────●─►  保护分支，仅 PR 合入，自动部署生产
                    /        /        /
develop      ──●──●─────●──●────●──●─►       集成分支，自动部署 staging
              /     \   /    \   /
feat/*    ──●─       ●─       ●─             功能分支
fix/*                                          缺陷修复
```

- 提交规范：Conventional Commits（`feat: 新增点赞接口`）
- PR 必经：1 人 review + CI 通过 + 单测覆盖率不低于 70%

#### 3.3.2 本地开发环境

```bash
# 1. 启动依赖服务
docker-compose -f deploy/docker-compose.dev.yml up -d  # MySQL 8 + Redis 7
cp .env.example .env

# 2. 启动后端服务
cd server && pnpm install
pnpm prisma generate && pnpm prisma migrate dev && pnpm prisma db seed
pnpm dev                  # 端口 3001

# 3. 启动管理后台
cd admin && pnpm install
pnpm dev                  # 端口 5173

# 4. 启动小程序
cd miniapp && npm install
npm run dev:h5            # 端口 5174（H5 调试）
npm run dev:mp-weixin     # 微信开发者工具
```

> **默认账号**：管理员 `admin` / `ChangeMe_123456`，测试环境微信登录使用 mock 模式。

#### 3.3.3 测试策略（金字塔）

| 层 | 工具 | 覆盖范围 | 目标 |
|---|---|---|---|
| 单元测试 | Vitest | Service / Util 纯函数 | 70% 行覆盖 |
| 集成测试 | Supertest + 测试 DB | Route ↔ DB 全链路 | 关键接口 100% |
| E2E（后台） | Playwright | 登录 → 审核 → 退出 | 主流程 |
| 小程序 | 微信开发者工具 + 真机 | 手工 + checklist | 每次发版 |
| 压测 | k6 / JMeter | 接口 RT、并发 | 见 §1.1 性能目标 |
| 安全 | OWASP ZAP + npm audit | 自动扫描 | 0 高危 |

#### 3.3.4 CI/CD 流水线

```yaml
# .github/workflows/ci.yml （示意）
on: [push, pull_request]
jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - checkout
      - setup pnpm + node 20
      - pnpm install --frozen-lockfile
      - pnpm lint
      - pnpm typecheck
      - pnpm test          # 含覆盖率
      - 上传 coverage 至 codecov
      - docker build -t campus-server:${{ github.sha }} packages/server
      - docker push (仅 main / develop)
  deploy:
    needs: ci
    if: github.ref == 'refs/heads/main'
    steps:
      - SSH 到生产机
      - docker pull + docker-compose up -d --no-deps server
      - 健康检查 /healthz；失败自动回滚到上一版本
```

#### 3.3.5 部署运维

| 项 | 方案 |
|---|---|
| 服务器 | 腾讯云 CVM 2c4g (Ubuntu 22.04) |
| 域名 / SSL | 域名备案 + Let's Encrypt 自动续期 |
| 进程守护 | PM2 集群模式 + `pm2-logrotate` |
| 日志 | Winston JSON → Loki / 文件按日切割保留 30 天 |
| 监控 | Prometheus 抓取 `/metrics` + Grafana 看板 |
| 告警 | 错误率 > 1% / RT > 1s / 磁盘 > 80% → 企微 webhook |
| 备份 | MySQL 每日 mysqldump 推 COS，保留 14 天；binlog 7 天 |
| 灰度 | Nginx 按 cookie 或 IP 分流；配置中心动态切流量比例 |

---

### 3.4 可扩展性设计建议

| 维度 | 当前方案 | 演进路线 |
|---|---|---|
| **多端适配** | 仅微信小程序 | uni-app 同源代码扩展至支付宝小程序 / 抖音 / H5 |
| **多校园** | 单实例 | 引入 `tenant_id` 字段；或独立部署每校 |
| **微服务拆分** | 单体 | 按域拆出 `content-svc`、`user-svc`、`moderation-svc`，gRPC 内部通信 |
| **缓存升级** | Redis 单机 | 哨兵 / Cluster；增加本地 LRU（lru-cache）作为二级缓存 |
| **搜索升级** | MySQL FULLTEXT | 接入 Meilisearch / Elasticsearch，支持高级排序 |
| **推荐系统** | 时间倒序 | 用户行为打点 → 离线计算偏好 → 协同过滤 / 向量召回 |
| **消息异步化** | 直接调用 | 引入 RabbitMQ / Redis Stream，解耦点赞同步、消息推送、审核 |
| **AI 审核** | 关键词 + 微信 API | 接入文本分类模型，预审准确率提升 |
| **教务集成** | 暂无 | 预留 OAuth 适配层，对接学校统一身份认证 |
| **国际化** | 中文 | i18n 框架预埋（vue-i18n），文案抽出至 JSON |

**架构演进的设计原则**
1. **接口先行**：所有跨模块调用走 Service 层抽象，未来替换数据源 / 缓存策略只改 Repository。
2. **事件驱动预埋**：业务关键节点（PostCreated / CommentCreated）通过 EventEmitter 发布，初期同步消费，未来切换 MQ 仅替换发布器。
3. **Feature Flag**：使用 `app_config` 表控制功能开关（如审核模式 pre/post），无需发版即可调整业务策略。
4. **配置外置**：DB 连接、限流阈值、敏感词等运行时可调，避免重启。

---

## 四、落地前查漏补缺（不含上线发布）

> 本节用于把方案从“架构说明”补强为“可直接进入开发”的施工清单。上线、灰度、备案、小程序发布审核、生产监控告警等事项不纳入本节范围。

### 4.1 必须统一的产品与状态口径

| 项 | 统一口径 | 开发影响 |
|---|---|---|
| 帖子审核模式 | v1 默认 **先审后发**：发帖后 `PENDING`，审核通过后普通列表可见。`app_config.review_mode` 预留 `pre/post` 切换。 | 列表接口默认只查 `APPROVED`；作者可在“我的帖子”看到待审/驳回。 |
| 评论审核模式 | v1 默认 **预扫后直接展示**：DFA/微信安全拒绝即失败，通过则 `NORMAL`。后台可删除。 | 评论不进入人工待审池，降低 MVP 复杂度。 |
| 帖子状态 | `0=PENDING` 待审，`1=APPROVED` 通过，`2=REJECTED` 驳回，`3=DELETED` 删除。 | Service、DTO、管理端筛选、错误码必须使用同一枚举。 |
| 评论状态 | `0=NORMAL` 正常，`1=DELETED` 删除，`2=HIDDEN` 后台隐藏。 | 不复用帖子审核状态，避免语义混乱。 |
| 用户状态 | `0=NORMAL` 正常，`1=BANNED` 禁用。 | 被禁用后登录失败，存量 token 进入 Redis blacklist。 |
| 文件状态 | `0=UPLOADED` 已上传未引用，`1=BOUND` 已绑定业务，`2=ORPHAN` 孤儿，`3=DELETED` 删除。 | 发帖成功后批量绑定文件；帖子删除后释放引用。 |
| 删除策略 | 业务表默认软删除；物理删除只由清理任务处理文件。 | 普通接口永远过滤删除态。 |

### 4.2 数据库设计补齐项

当前 DDL 是节选，进入实现前需要以 `schema.prisma` 为准补齐全部模型。建议保留文档 DDL 作为设计说明，但真正施工使用 Prisma schema。

| 表 | 必补字段 / 约束 | 关键索引 |
|---|---|---|
| `user` | v1 使用同表承载普通用户与管理员：普通用户有 `wechat_openid`，管理员有 `admin_account/password_hash`，通过互斥校验保证身份类型清晰。若后续管理员体系复杂化，再拆分 `admin_user` 表。 | `UNIQUE(wechat_openid)`，`UNIQUE(admin_account)`，`idx_role_status(role,status)` |
| `comment` | `comment_id`、`post_id`、`user_id`、`parent_id`、`reply_to_user_id`、`content`、`like_count`、`status`、`create_time`、`update_time`。二级回复的 `parent_id` 必须指向一级评论。 | `idx_post_parent(post_id,parent_id,create_time)`，`idx_user(user_id,create_time)` |
| `post_like` | `like_id`、`post_id`、`user_id`、`status`、`create_time`、`update_time`。取消点赞可软置 `status=0`，也可物理删除；v1 建议软状态便于审计。 | `UNIQUE(user_id,post_id)`，`idx_post(post_id,status)` |
| `comment_like` | 若评论支持点赞，需要单独建表；若 v1 不支持，应从 UI 和接口中明确去掉。 | `UNIQUE(user_id,comment_id)` |
| `user_follow` | `follow_id`、`follower_id`、`following_id`、`status`、`create_time`、`update_time`。禁止自己关注自己。 | `UNIQUE(follower_id,following_id)`，`idx_following(following_id,status)` |
| `notice` | `notice_id`、`title`、`content`、`type`、`status`、`is_top`、`start_time`、`end_time`、`operator_id`。 | `idx_status_time(status,start_time,end_time)` |
| `admin_log` | `log_id`、`admin_id`、`action`、`target_type`、`target_id`、`before_json`、`after_json`、`ip`、`user_agent`、`create_time`。 | `idx_admin_time(admin_id,create_time)`，`idx_target(target_type,target_id)` |
| `report` | `report_id`、`reporter_id`、`target_type`、`target_id`、`reason_type`、`reason_text`、`status`、`handler_id`、`handle_result`、`create_time`、`handle_time`。 | `idx_status(status,create_time)`，`idx_target(target_type,target_id)` |
| `sensitive_word` | `word_id`、`word`、`level`、`category`、`status`、`operator_id`、`create_time`、`update_time`。 | `UNIQUE(word)`，`idx_status(status)` |
| `app_config` | `config_key`、`config_value`、`value_type`、`description`、`is_public`、`update_time`。 | `PRIMARY KEY(config_key)` |

补充约束：
- 所有外键是否用数据库强约束需统一。v1 推荐 Prisma relation + 关键唯一索引，避免频繁软删除导致外键处理复杂。
- 所有 ID 类型统一为 `BIGINT UNSIGNED`；所有对外 API / shared DTO / OpenAPI schema 中的 ID 一律定义为字符串，避免小程序 / JavaScript number 精度丢失。
- `post.image_urls` 与 `file` 表存在冗余，必须由 Service 保证一致：发帖提交 URL 后校验文件归属当前用户，绑定成功后写入 `image_urls` 快照。
- MySQL 8 中文全文索引的 `ngram` 需要数据库插件/配置确认；若本地环境不可用，MVP 搜索先降级为标题前缀/LIKE，并在代码中隔离搜索仓储。

### 4.3 API 契约补齐清单

现有文档只有接口示例，开发前应补 `docs/openapi.yaml`。最低需要覆盖以下接口：

| 模块 | 接口 |
|---|---|
| Auth | `POST /api/v1/auth/wechat-login`、`POST /api/v1/auth/admin-login`、`POST /api/v1/auth/refresh`、`POST /api/v1/auth/logout` |
| User | `GET /api/v1/users/me`、`PUT /api/v1/users/me`、`GET /api/v1/users/:id`、`GET /api/v1/users/:id/posts`、`GET /api/v1/users/:id/followers`、`GET /api/v1/users/:id/following` |
| Post | `GET /api/v1/posts`、`GET /api/v1/posts/:id`、`POST /api/v1/posts`、`PUT /api/v1/posts/:id`、`DELETE /api/v1/posts/:id`、`GET /api/v1/posts/mine` |
| Comment | `GET /api/v1/posts/:postId/comments`、`POST /api/v1/posts/:postId/comments`、`DELETE /api/v1/comments/:id` |
| Interaction | `POST /api/v1/posts/:id/like`、`DELETE /api/v1/posts/:id/like`、`POST /api/v1/users/:id/follow`、`DELETE /api/v1/users/:id/follow` |
| Category | `GET /api/v1/categories` |
| Message | `GET /api/v1/messages`、`GET /api/v1/messages/unread-count`、`PUT /api/v1/messages/:id/read`、`PUT /api/v1/messages/read-all` |
| File | `POST /api/v1/files/upload`、`GET /api/v1/files/:id`、`DELETE /api/v1/files/:id` |
| Report | `POST /api/v1/reports` |
| Admin | `GET /api/v1/admin/posts`、`PUT /api/v1/admin/posts/:id/status`、`GET /api/v1/admin/comments`、`DELETE /api/v1/admin/comments/:id`、`GET /api/v1/admin/users`、`PUT /api/v1/admin/users/:id/status`、`POST /api/v1/admin/categories`、`PUT /api/v1/admin/categories/:id`、`DELETE /api/v1/admin/categories/:id`、`GET /api/v1/admin/reports`、`PUT /api/v1/admin/reports/:id/status`、`GET /api/v1/admin/logs` |

接口统一要求：
- 成功响应必须包含 `code/message/data/traceId`，即使 `data=null` 也保持结构一致。
- 分页请求统一限制：`page >= 1`，`size` 默认 20，最大 50。
- 列表 DTO 必须明确 `isLiked/isFollowed` 只在登录态可计算，游客态返回 `false` 或省略需统一。建议返回 `false`。
- 写接口必须定义幂等语义：重复点赞、重复关注、重复取消均返回 `code=0` 和当前状态。
- 管理端接口必须在 OpenAPI 中标注角色要求：`moderator` 或 `admin`。

### 4.4 Redis 与异步一致性补强

点赞方案采用 Redis Stream，并用 Lua 保证关键操作原子化：

```text
点赞：
1. 校验 JWT 与用户状态
2. Lua 原子执行：
   - 若 like:{userId}:{postId} 已存在，返回当前计数
   - SET like:{userId}:{postId}=1
   - INCR post:{postId}:likeCount
   - XADD like_sync_stream userId postId op=like ts
3. 返回最新 likeCount

取消点赞：
1. Lua 原子执行：
   - 若 like key 不存在，返回当前计数
   - DEL like key
   - DECR post:{postId}:likeCount，但不得小于 0
   - XADD like_sync_stream op=unlike
2. 返回最新 likeCount
```

Worker 处理规则：
- 使用 Redis Stream consumer group，消费成功后 ACK。
- MySQL 落库按 `(user_id, post_id)` UPSERT，避免重复消息导致计数错误。
- `post.like_count` 不建议每条消息 `+1/-1` 盲加；更稳妥是批量聚合后更新，或周期性以 `post_like` 有效行数校准。
- 启动时若 Redis 计数缺失，从 MySQL 回源并设置缓存。

### 4.5 DTO 与共享类型约定

`packages/shared` 不只放类型，也应沉淀稳定枚举和响应结构：

```ts
export type ID = string;

export enum PostStatus {
  Pending = 0,
  Approved = 1,
  Rejected = 2,
  Deleted = 3,
}

export interface ApiResponse<T> {
  code: number;
  message: string;
  data: T;
  traceId: string;
}

export interface PageResult<T> {
  list: T[];
  total: number;
  page: number;
  size: number;
  hasMore: boolean;
}
```

约定：
- 后端内部用 Prisma 类型，接口出入参用 DTO，不直接暴露数据库模型。
- DTO 字段统一 camelCase；数据库字段统一 snake_case；转换在 Repository/Service 边界完成。
- OpenAPI、shared DTO、zod schema 三者要有单一来源。v1 建议先用 zod 定义请求校验，再从 zod 推导 TS 类型。

### 4.6 MVP 开发边界

为了避免第一阶段范围膨胀，建议 v1 MVP 明确包含：

| 包含 | 暂缓 |
|---|---|
| 微信登录、管理员登录 | 第三方统一身份认证 |
| 帖子列表、详情、发布、编辑、删除 | 推荐流、复杂排序 |
| 一级/二级评论、删除 | 评论点赞 |
| 帖子点赞、取消点赞 | 收藏 |
| 关注、取消关注、关注列表 | 复杂好友关系 |
| 分类列表、后台分类 CRUD | 多级分类 |
| 图片上传、缩略图、文件绑定 | 对象存储迁移 |
| DFA 敏感词、微信内容安全封装 | AI 审核 |
| 后台帖子审核、评论删除、用户封禁、举报处理 | 多角色细粒度权限矩阵 |
| 站内消息、未读数 | 微信订阅消息推送 |

### 4.7 建议开发顺序

1. ✅ 搭建项目基础设施：env、logger、response、error、traceId。
2. ✅ 落 `schema.prisma`、迁移、seed：管理员账号、默认分类、基础配置。
3. ✅ 实现 Auth/User：微信登录 mock 适配、本地管理员登录、JWT、RBAC。
4. ✅ 实现 File/Storage：上传、格式校验、缩略图、文件归属校验。
5. ✅ 实现 Post/Category：发帖、列表、详情、审核状态流转。
6. ✅ 实现 Comment/Message：评论树、评论通知、未读数。
7. ✅ 实现 Like/Follow：Redis 原子操作、异步同步、幂等测试。
8. ✅ 实现 Admin：审核、封禁、分类、举报、操作日志。
9. ✅ 补齐 miniapp 与 admin 页面，最后做联调和自动化测试。

### 4.8 测试补充清单

| 层级 | 必测场景 |
|---|---|
| Service 单测 | 发帖状态、审核流转、用户封禁、资源归属、敏感词拒绝、重复点赞/取消点赞、重复关注/取消关注 |
| Repository 集成测试 | 关键索引查询、分页、软删除过滤、唯一约束冲突、事务回滚 |
| API 集成测试 | 鉴权缺失、角色不足、参数错误、统一响应结构、traceId、限流 |
| Worker 测试 | Redis Stream 重复投递、ACK 后不重复、失败重试、MySQL 与 Redis 计数校准 |
| 前端组件测试 | 帖子卡片、图片上传、评论项、空状态、权限按钮显隐 |

---

## 附录 A · 风险登记册（节选）

| 风险 | 概率 | 影响 | 缓解措施 |
|---|---|---|---|
| 微信 jscode2session 限流 | 中 | 高 | 服务端缓存 session 5 分钟；失败降级到游客态 |
| 图片大量上传致磁盘空间不足 | 中 | 中 | 客户端压缩 + 单帖图片数量限制 9 张 + 磁盘使用率 > 80% 告警 + 孤儿文件定时清理 |
| 学生群体瞬时高并发（讲座 / 公告） | 高 | 中 | 公告页强缓存 + 网关静态化 + Redis 列表缓存 |
| 违规内容引发监管风险 | 低 | 高 | 双重审核 + 敏感词库定期更新 + 操作日志留存 6 个月 |
| 单点故障 | 中 | 高 | v2 阶段引入双机热备 + 数据库主从 |

---

## 附录 B · 与论文方案的主要差异（工程化补强）

| 论文 | 工程方案 | 原因 |
|---|---|---|
| 裸 mysql2 | Prisma 7 ORM | 类型安全 + Migration 管理 + 防注入 |
| Node.js 14 | Node.js 20 LTS | 14 已 EOL，安全合规 |
| 图片 URL 字段（VARCHAR） | JSON 数组 + 独立 file 表 | 多图灵活，孤儿文件可清理 |
| 仅 MySQL | + Redis | 计数器、缓存、限流必需 |
| 后端中转图片 | 本地磁盘 + Express 静态服务 | 零外部费用，运维简单，校园场景可控 |
| 关键词搜索（LIKE） | MySQL FULLTEXT ngram | LIKE 全表扫不可接受 |
| 鉴权 JWT 但未提撤销 | JWT + Redis 黑名单 | 用户被封禁后存量 token 立即失效 |
| 未提消息表 | message 表 + 合并去重 | 通知是论文 3.2.1 第 4 条提到的功能 |
| 未提敏感词与举报机制 | DFA + 举报表 + 微信内容安全 | 论文 3.3.2 提到但实现未展开 |
| 接口路径 `/api/likes` | `/api/v1/posts/:id/like` | 更符合 RESTful 资源建模 + 版本化 |
| 未提资料审核 | profile-review 模块 | bio/昵称/性别/年龄修改需管理员审核 |
| 未提分类权限 | allowUserPost 字段 | 活动公告等分类仅管理员可发布 |
| 未提隐私设置 | showGender/showAge | 用户可控制个人信息可见性 |
| 未提评论敏感词过滤 | 可配置开关 + 5 分钟缓存 | 管理后台可动态启用/关闭 |
| 未提举报结果通知 | 系统消息通知举报人 | 处理结果实时通知，提升用户体验 |
| 未提系统配置管理 | app_config 表 + 管理后台 | 动态配置，无需重启服务 |

> 施工时以本落地方案和后续 `schema.prisma` / `docs/openapi.yaml` 为准；`paper.txt` 用作论文叙述材料，涉及“已实现、已测试”的表述需在答辩或归档前按真实开发进度修订。

---

> 本方案文档可作为团队 kickoff 的基线材料。建议按章节拆分为独立 Wiki 页面：架构图入 `docs/architecture.md`，API 入 `docs/openapi.yaml`，DB 入 `docs/db-schema.md`，便于后续维护。
